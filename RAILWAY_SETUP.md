# 🚂 Настройка Railway - пошагово с скриншотами

## Шаг 1: Регистрация

1. Откройте https://railway.app
2. Нажмите **"Login"**
3. Выберите **"Login with GitHub"**
4. Разрешите доступ Railway к вашему GitHub

## Шаг 2: Создание проекта

1. После входа нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите репозиторий `task-tracker-cute`
4. Нажмите на него
5. Railway начнет автоматический деплой (пока без настроек)

## Шаг 3: Добавление PostgreSQL

1. В проекте Railway нажмите **"+ New"** (внизу слева)
2. Выберите **"Database"**
3. Выберите **"PostgreSQL"**
4. Railway автоматически создаст базу данных
5. Дождитесь завершения (статус "Healthy")

## Шаг 4: Настройка API сервиса

1. Найдите сервис с названием вашего репозитория (обычно первый)
2. Нажмите на него
3. Откройте вкладку **"Settings"** (слева)
4. Прокрутите до **"Service Settings"**

### Настройте:

**Root Directory:**
```
apps/api
```

**Build Command:**
```
pnpm install && pnpm build
```

**Start Command:**
```
pnpm prisma generate && pnpm prisma migrate deploy && pnpm start:prod
```

5. Нажмите **"Save"**

## Шаг 5: Переменные окружения

1. В настройках сервиса откройте вкладку **"Variables"**
2. Нажмите **"+ New Variable"** для каждой переменной:

### DATABASE_URL
```
${{Postgres.DATABASE_URL}}
```
(Это автоматически подставит URL из PostgreSQL сервиса)

### JWT_SECRET
```
<вставьте сгенерированную строку>
```
(Используйте команду: `openssl rand -base64 32`)

### PORT
```
3001
```

### FRONTEND_URL
```
https://your-app.vercel.app
```
(Заполните после деплоя на Vercel - пока можно оставить пустым)

### RESEND_API_KEY (опционально)
```
re_xxxxx
```
(Только если нужны email приглашения)

3. После добавления всех переменных Railway автоматически перезапустит сервис

## Шаг 6: Получение URL

1. В настройках сервиса откройте вкладку **"Networking"**
2. Нажмите **"Generate Domain"**
3. Скопируйте URL (например: `https://your-api.up.railway.app`)

## Шаг 7: Проверка

Откройте в браузере: `https://your-api.up.railway.app/health`

Должно вернуть: `{"ok":true}`

## ✅ Готово!

API задеплоен и работает!

