# 🚂 Полная настройка Railway - API + Web + PostgreSQL

## Архитектура

```
Railway Project:
  ├─ PostgreSQL (база данных)
  ├─ API (NestJS) - порт 3001
  └─ Web (Next.js) - порт 3000
```

## Шаг 1: Регистрация

1. Откройте https://railway.app
2. Нажмите **"Login"**
3. Выберите **"Login with GitHub"**
4. Разрешите доступ Railway к вашему GitHub

## Шаг 2: Создание проекта

1. После входа нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите репозиторий `tasktracker`
4. Нажмите на него
5. Railway начнет автоматический деплой (пока без настроек)

## Шаг 3: Добавление PostgreSQL

1. В проекте Railway нажмите **"+ New"** (внизу слева)
2. Выберите **"Database"**
3. Выберите **"PostgreSQL"**
4. Railway автоматически создаст базу данных
5. Дождитесь завершения (статус "Healthy")

## Шаг 4: Настройка API сервиса

1. Найдите сервис с названием вашего репозитория (обычно первый)
2. Переименуйте его в **"api"** (для удобства)
3. Нажмите на него
4. Откройте вкладку **"Settings"** (слева)
5. Прокрутите до **"Service Settings"**

### Настройте:

**Root Directory:**
```
apps/api
```

**Build Command:**
```
pnpm install && pnpm build
```

**Start Command:**
```
pnpm prisma generate && pnpm prisma migrate deploy && pnpm start:prod
```

6. Нажмите **"Save"**

## Шаг 5: Переменные окружения для API

1. В настройках API сервиса откройте вкладку **"Variables"**
2. Нажмите **"+ New Variable"** для каждой переменной:

### DATABASE_URL
```
${{Postgres.DATABASE_URL}}
```
(Это автоматически подставит URL из PostgreSQL сервиса)

### JWT_SECRET
```
DjLNj8omoOmhO0c2WuOC6pIqlO9FUlop+C3X4lwkJ9Y=
```

### PORT
```
3001
```

### FRONTEND_URL
```
https://ваш-web-url.up.railway.app
```
(Заполните после создания Web сервиса - пока можно оставить пустым)

### RESEND_API_KEY (опционально)
```
re_xxxxx
```
(Только если нужны email приглашения)

3. После добавления всех переменных Railway автоматически перезапустит сервис

## Шаг 6: Получение URL API

1. В настройках API сервиса откройте вкладку **"Networking"**
2. Нажмите **"Generate Domain"**
3. Скопируйте URL (например: `https://api-xxx.up.railway.app`)

## Шаг 7: Создание Web сервиса

1. В проекте Railway нажмите **"+ New"** (внизу слева)
2. Выберите **"GitHub Repo"**
3. Выберите тот же репозиторий `tasktracker`
4. Railway создаст новый сервис

## Шаг 8: Настройка Web сервиса

1. Найдите новый сервис (или переименуйте его в **"web"**)
2. Нажмите на него
3. Откройте вкладку **"Settings"** (слева)
4. Прокрутите до **"Service Settings"**

### Настройте:

**Root Directory:**
```
apps/web
```

**Build Command:**
```
pnpm install && pnpm build
```

**Start Command:**
```
pnpm start
```

5. Нажмите **"Save"**

## Шаг 9: Переменные окружения для Web

1. В настройках Web сервиса откройте вкладку **"Variables"**
2. Нажмите **"+ New Variable"** для каждой переменной:

### NEXT_PUBLIC_USE_API
```
true
```

### NEXT_PUBLIC_API_URL
```
https://ваш-api-url.up.railway.app
```
(URL из шага 6)

3. После добавления переменных Railway автоматически перезапустит сервис

## Шаг 10: Получение URL Web

1. В настройках Web сервиса откройте вкладку **"Networking"**
2. Нажмите **"Generate Domain"**
3. Скопируйте URL (например: `https://web-xxx.up.railway.app`)

## Шаг 11: Обновление FRONTEND_URL в API

1. Вернитесь в настройки **API** сервиса
2. Откройте **"Variables"**
3. Найдите переменную **FRONTEND_URL**
4. Обновите её на URL из шага 10:
   ```
   https://ваш-web-url.up.railway.app
   ```

## Шаг 12: Проверка

### Проверка API:
Откройте в браузере: `https://ваш-api-url.up.railway.app/health`

Должно вернуть: `{"ok":true}`

### Проверка Web:
Откройте в браузере: `https://ваш-web-url.up.railway.app`

Должна открыться главная страница Task Tracker!

## ✅ Готово!

Всё задеплоено на Railway:
- ✅ PostgreSQL база данных
- ✅ API (NestJS)
- ✅ Web (Next.js)

Теперь ваше приложение доступно в интернете!

