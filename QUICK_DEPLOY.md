# 🚀 Быстрый деплой в интернет

## Самый простой способ (рекомендуется)

### Шаг 1: Деплой API на Railway (5 минут)

1. **Зарегистрируйтесь на Railway**
   - Перейдите на [railway.app](https://railway.app)
   - Войдите через GitHub

2. **Создайте новый проект**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий `task-tracker-cute`

3. **Добавьте PostgreSQL базу данных**
   - В проекте нажмите "+ New"
   - Выберите "Database" → "PostgreSQL"
   - Railway автоматически создаст базу

4. **Добавьте API сервис**
   - В проекте нажмите "+ New"
   - Выберите "GitHub Repo" → ваш репозиторий
   - В настройках сервиса:
     - **Root Directory**: `apps/api`
     - **Build Command**: `pnpm install && pnpm build`
     - **Start Command**: `pnpm start:prod`

5. **Настройте переменные окружения**
   - В настройках API сервиса → "Variables"
   - Добавьте:
     ```
     DATABASE_URL=${{Postgres.DATABASE_URL}}
     JWT_SECRET=ваш-секретный-ключ-минимум-32-символа-случайные-буквы-и-цифры
     PORT=3001
     RESEND_API_KEY=re_xxxxx (опционально, для email)
     FRONTEND_URL=https://ваш-домен.vercel.app (заполните после деплоя веба)
     ```
   - `DATABASE_URL` автоматически подставится из PostgreSQL сервиса

6. **Примените миграции базы данных**
   - В настройках API сервиса → "Settings" → "Deploy"
   - Добавьте в "Deploy Command": `pnpm prisma migrate deploy && pnpm start:prod`
   - Или выполните локально:
     ```bash
     cd apps/api
     DATABASE_URL=<ваш-url-из-railway> pnpm prisma migrate deploy
     ```

7. **Получите URL API**
   - После деплоя Railway даст URL типа: `https://your-api.up.railway.app`
   - Скопируйте этот URL

### Шаг 2: Деплой Web на Vercel (3 минуты)

1. **Зарегистрируйтесь на Vercel**
   - Перейдите на [vercel.com](https://vercel.com)
   - Войдите через GitHub

2. **Импортируйте проект**
   - Нажмите "Add New" → "Project"
   - Выберите ваш репозиторий `task-tracker-cute`

3. **Настройте проект**
   - **Framework Preset**: Next.js (автоматически)
   - **Root Directory**: `apps/web`
   - **Build Command**: `cd ../.. && pnpm install && cd apps/web && pnpm build`
   - Или создайте `vercel.json` в корне проекта (см. ниже)

4. **Добавьте переменные окружения**
   - В настройках проекта → "Environment Variables"
   - Добавьте:
     ```
     NEXT_PUBLIC_USE_API=true
     NEXT_PUBLIC_API_URL=https://your-api.up.railway.app
     ```
   - Замените `your-api.up.railway.app` на ваш URL из Railway

5. **Деплой**
   - Нажмите "Deploy"
   - Дождитесь завершения
   - Получите URL: `https://your-app.vercel.app`

6. **Обновите FRONTEND_URL в Railway**
   - Вернитесь в Railway
   - В переменных окружения API обновите:
     ```
     FRONTEND_URL=https://your-app.vercel.app
     ```
   - Перезапустите API сервис

### Шаг 3: Проверка работы

1. **Проверьте API**: `https://your-api.up.railway.app/health`
   - Должен вернуть: `{"ok":true}`

2. **Откройте веб-приложение**: `https://your-app.vercel.app`
   - Должна открыться главная страница

3. **Проверьте вход**
   - Перейдите на `/login`
   - Войдите через dev-login
   - Создайте workspace

## Файлы для деплоя

### vercel.json (в корне проекта)

```json
{
  "buildCommand": "cd apps/web && pnpm install && pnpm build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs"
}
```

### railway.json (опционально, для Railway)

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd apps/api && pnpm prisma migrate deploy && pnpm start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

## Настройка Email (опционально)

1. Зарегистрируйтесь на [resend.com](https://resend.com)
2. Создайте API ключ
3. Добавьте в Railway переменную: `RESEND_API_KEY=re_xxxxx`

## Troubleshooting

### API не запускается
- Проверьте, что `DATABASE_URL` правильно настроен
- Убедитесь, что миграции применены: `pnpm prisma migrate deploy`

### CORS ошибки
- Проверьте `FRONTEND_URL` в Railway
- Убедитесь, что URL веб-приложения правильный

### База данных не подключается
- Проверьте `DATABASE_URL` в Railway
- Убедитесь, что PostgreSQL сервис запущен

## Следующие шаги

1. Настройте кастомный домен (опционально)
2. Настройте мониторинг
3. Настройте бэкапы базы данных

