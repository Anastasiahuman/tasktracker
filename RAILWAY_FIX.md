# 🔧 Исправление ошибки "pnpm: command not found" в Railway

## Проблема

Railway пытается использовать Dockerfile, но получает ошибку:
```
/bin/bash: line 1: pnpm: command not found
```

## Решение

### Вариант 1: Использовать NIXPACKS (РЕКОМЕНДУЮ!)

NIXPACKS автоматически определит pnpm и monorepo структуру.

**В Railway Settings → Service Settings:**

1. **Builder:** выберите `NIXPACKS` из выпадающего списка
2. **Root Directory:** `apps/api`
3. **Build Command:** 
   ```
   pnpm install && pnpm build
   ```
4. **Start Command:**
   ```
   pnpm prisma generate && pnpm prisma migrate deploy && pnpm start:prod
   ```
5. **Build Command и Start Command должны быть заполнены!**

### Вариант 2: Использовать Dockerfile

Если Railway все равно пытается использовать Dockerfile:

**В Railway Settings → Service Settings:**

1. **Builder:** `Dockerfile`
2. **Root Directory:** `.` (корень проекта, НЕ apps/api!)
3. **Dockerfile Path:** `Dockerfile` (или оставьте пустым)
4. **Build Command:** ОСТАВЬТЕ ПУСТЫМ
5. **Start Command:** ОСТАВЬТЕ ПУСТЫМ

⚠️ **Важно:** Если используете Dockerfile, Build Command и Start Command должны быть ПУСТЫМИ, иначе Railway попытается выполнить обе команды!

## Проверка

После изменения настроек:
1. Railway автоматически перезапустит сборку
2. Проверьте логи сборки
3. Если ошибка повторяется, убедитесь, что Builder = NIXPACKS

## Почему NIXPACKS лучше?

- ✅ Автоматически определяет pnpm
- ✅ Понимает monorepo структуру
- ✅ Не требует Dockerfile
- ✅ Проще в настройке

