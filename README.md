# Task Tracker Monorepo

Монорепо для управления задачами с Next.js фронтендом, NestJS API и React Admin backoffice.

## ✨ Новые возможности

- ✅ **Исполнители задач** - назначение задач участникам команды
- ✅ **Приглашения по email** - приглашайте участников в workspace
- ✅ **Регистрация по приглашению** - новые пользователи могут зарегистрироваться по ссылке
- ✅ **Email уведомления** - автоматическая отправка приглашений через Resend
- ✅ **Управление workspace** - страница настроек с участниками и приглашениями
- ✅ **Регистрация и вход** - полноценная система аутентификации
- 🚀 **Готово к деплою** - инструкции в `DEPLOYMENT.md`
- 📖 **Быстрый деплой** - пошаговая инструкция в `START_DEPLOY.md` (10 минут)

## 📧 Настройка Email

Для работы приглашений нужно настроить Resend. Подробные инструкции в `SETUP_EMAIL.md`.

Кратко:
1. Зарегистрируйтесь на [resend.com](https://resend.com)
2. Создайте API ключ
3. Добавьте в `apps/api/.env`: `RESEND_API_KEY=re_xxxxx`

## Структура

- `apps/web` - Next.js приложение (фронтенд)
- `apps/api` - NestJS API (бэкенд)
- `apps/admin` - React Admin (backoffice)
- `packages/shared` - общие типы и утилиты

## Требования

- Node.js >= 18.0.0
- pnpm >= 8.0.0
- Docker Desktop (для PostgreSQL)

## Быстрый старт

### 1. Установка зависимостей

```bash
pnpm install
```

### 2. Запуск PostgreSQL через Docker

**ВАЖНО**: API не запустится без работающей базы данных!

```bash
# Запустить Docker Desktop (если еще не запущен)
# Затем выполнить:
docker-compose up -d

# Проверить, что контейнер запущен:
docker ps | grep postgres
```

### 3. Настройка базы данных

```bash
cd apps/api

# Создать .env файл (если еще нет):
# DATABASE_URL="postgresql://tasktracker:tasktracker@localhost:5432/task_tracker?schema=public"
# JWT_SECRET="your-secret-key-here"
# PORT=3001

# Применить миграции Prisma:
pnpm prisma generate
pnpm prisma migrate dev
```

### 4. Запуск приложений

В отдельных терминалах:

```bash
# API (порт 3001)
pnpm dev:api

# Web (порт 3000)
pnpm dev:web

# Admin (порт 3002)
pnpm dev:admin
```

Или все сразу:

```bash
pnpm dev
```

## Проверка работы

### API Health Check

```bash
curl http://localhost:3001/health
# Ожидаемый ответ: {"ok":true}

curl http://localhost:3001/
# Ожидаемый ответ: информация об API
```

### Web App

Откройте [http://localhost:3000](http://localhost:3000)

### Admin Backoffice

Откройте [http://localhost:3002](http://localhost:3002)

## Устранение проблем

### Ошибка: "Can't reach database server at localhost:5432"

**Причина**: PostgreSQL не запущен или Docker не работает.

**Решение**:
1. Убедитесь, что Docker Desktop запущен
2. Выполните `docker-compose up -d`
3. Проверьте: `docker ps | grep postgres`

### Ошибка: "Failed to connect to localhost port 3001"

**Причина**: API сервер не запущен или не может подключиться к БД.

**Решение**:
1. Убедитесь, что PostgreSQL запущен (см. выше)
2. Проверьте `.env` файл в `apps/api/`
3. Запустите `pnpm dev:api` и проверьте логи на ошибки

### Ошибка: "docker: command not found"

**Причина**: Docker не установлен или не в PATH.

**Решение**:
1. Установите Docker Desktop: https://www.docker.com/products/docker-desktop
2. Убедитесь, что Docker Desktop запущен
3. Перезапустите терминал

## Environment Variables

### apps/api/.env

```env
DATABASE_URL="postgresql://tasktracker:tasktracker@localhost:5432/task_tracker?schema=public"
JWT_SECRET="your-secret-key-change-in-production"
PORT=3001
RESEND_API_KEY="re_xxxxxxxxxxxxx"  # Опционально, для приглашений
FRONTEND_URL="http://localhost:3000"  # Для ссылок в email
```

### apps/web/.env.local

```env
NEXT_PUBLIC_USE_API=true
NEXT_PUBLIC_API_URL=http://localhost:3001
```

### apps/admin/.env

```env
VITE_API_URL=http://localhost:3001
```

## Команды

- `pnpm dev` - запустить все приложения
- `pnpm dev:api` - запустить только API
- `pnpm dev:web` - запустить только Web
- `pnpm dev:admin` - запустить только Admin
- `pnpm build` - собрать все приложения
- `pnpm lint` - проверить код линтером
