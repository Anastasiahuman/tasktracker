# 🚀 Пошаговый деплой - выполняйте по порядку

## ✅ Шаг 1: Подготовка (ВЫПОЛНЕНО)

- [x] Все файлы закоммичены
- [x] Конфигурации созданы (railway.json, vercel.json)
- [x] CORS настроен для production

## 📋 Шаг 2: GitHub репозиторий

### Если репозитория еще нет:

1. Откройте [github.com](https://github.com) и войдите
2. Нажмите **"New repository"**
3. Название: `task-tracker-cute`
4. Выберите **Public** (или Private)
5. **НЕ** добавляйте README, .gitignore, license (они уже есть)
6. Нажмите **"Create repository"**

### Подключите локальный репозиторий:

```bash
cd /Users/anastasiaamanova/task-tracker-cute
git remote add origin https://github.com/ВАШ-USERNAME/task-tracker-cute.git
git branch -M main
git push -u origin main
```

**Замените `ВАШ-USERNAME` на ваш GitHub username!**

## 🚂 Шаг 3: Деплой API на Railway

### 3.1 Регистрация и создание проекта

1. Откройте [railway.app](https://railway.app)
2. Нажмите **"Login"** → выберите **"Login with GitHub"**
3. После входа нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Найдите и выберите репозиторий `task-tracker-cute`
6. Railway автоматически начнет деплой (пока без настроек)

### 3.2 Добавление PostgreSQL базы данных

1. В проекте Railway нажмите **"+ New"**
2. Выберите **"Database"** → **"PostgreSQL"**
3. Railway автоматически создаст базу данных
4. **Скопируйте** `DATABASE_URL` из настроек базы (понадобится позже)

### 3.3 Настройка API сервиса

1. В проекте Railway найдите сервис с вашим репозиторием
2. Нажмите на него, откройте **"Settings"**
3. Настройте:
   - **Root Directory**: `apps/api`
   - **Build Command**: `pnpm install && pnpm build`
   - **Start Command**: `pnpm prisma generate && pnpm prisma migrate deploy && pnpm start:prod`

### 3.4 Переменные окружения для API

1. В настройках API сервиса откройте **"Variables"**
2. Нажмите **"+ New Variable"**
3. Добавьте по одной:

   **DATABASE_URL:**
   ```
   ${{Postgres.DATABASE_URL}}
   ```
   (Railway автоматически подставит URL из PostgreSQL)

   **JWT_SECRET:**
   ```
   ваш-случайный-секрет-минимум-32-символа-abcdefghijklmnopqrstuvwxyz1234567890
   ```
   Сгенерируйте случайную строку:
   ```bash
   openssl rand -base64 32
   ```

   **PORT:**
   ```
   3001
   ```

   **FRONTEND_URL:**
   ```
   https://your-app.vercel.app
   ```
   (Заполните после деплоя веба на Vercel)

   **RESEND_API_KEY (опционально):**
   ```
   re_xxxxx
   ```
   (Только если нужны email приглашения)

### 3.5 Деплой и получение URL

1. Railway автоматически начнет деплой после сохранения настроек
2. Дождитесь завершения (может занять 3-5 минут)
3. После успешного деплоя откройте **"Settings"** → **"Networking"**
4. Нажмите **"Generate Domain"**
5. **Скопируйте URL** (например: `https://your-api.up.railway.app`)

### 3.6 Проверка API

Откройте в браузере: `https://your-api.up.railway.app/health`

Должно вернуть: `{"ok":true}`

## ▲ Шаг 4: Деплой Web на Vercel

### 4.1 Регистрация и импорт проекта

1. Откройте [vercel.com](https://vercel.com)
2. Нажмите **"Sign Up"** → выберите **"Continue with GitHub"**
3. После входа нажмите **"Add New"** → **"Project"**
4. Найдите репозиторий `task-tracker-cute`
5. Нажмите **"Import"**

### 4.2 Настройка проекта

1. В настройках проекта:
   - **Framework Preset**: Next.js (автоматически)
   - **Root Directory**: `apps/web` (или оставьте по умолчанию, vercel.json настроен)
   - **Build Command**: оставьте по умолчанию (или `cd apps/web && pnpm build`)
   - **Install Command**: `pnpm install`

### 4.3 Переменные окружения для Web

1. В настройках проекта откройте **"Environment Variables"**
2. Добавьте:

   **NEXT_PUBLIC_USE_API:**
   ```
   true
   ```

   **NEXT_PUBLIC_API_URL:**
   ```
   https://your-api.up.railway.app
   ```
   (Замените на реальный URL из Railway!)

### 4.4 Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения (может занять 2-3 минуты)
3. После успешного деплоя **скопируйте URL** (например: `https://your-app.vercel.app`)

### 4.5 Проверка Web

Откройте в браузере: `https://your-app.vercel.app`

Должна открыться главная страница Task Tracker.

## 🔄 Шаг 5: Обновление FRONTEND_URL

1. Вернитесь в Railway
2. Откройте настройки API сервиса → **"Variables"**
3. Найдите переменную `FRONTEND_URL`
4. Обновите значение на URL из Vercel:
   ```
   https://your-app.vercel.app
   ```
5. Railway автоматически перезапустит API

## ✅ Шаг 6: Финальная проверка

1. **API Health**: `https://your-api.up.railway.app/health` → `{"ok":true}`
2. **Web приложение**: `https://your-app.vercel.app` → открывается главная страница
3. **Вход**: `https://your-app.vercel.app/login` → форма входа работает
4. **Создание workspace**: войдите и создайте workspace

## 🎉 Готово!

Ваше приложение теперь доступно в интернете!

- **API**: `https://your-api.up.railway.app`
- **Web**: `https://your-app.vercel.app`

## 📝 Полезные ссылки

- Railway Dashboard: https://railway.app/dashboard
- Vercel Dashboard: https://vercel.com/dashboard
- Логи Railway: Settings → Deployments → View Logs
- Логи Vercel: Deployments → View Function Logs

## 🆘 Если что-то не работает

1. Проверьте логи в Railway и Vercel
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что миграции применены (Railway делает это автоматически)
4. Убедитесь, что `FRONTEND_URL` в Railway совпадает с URL Vercel

