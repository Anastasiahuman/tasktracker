# 🔍 БЫСТРАЯ ПРОВЕРКА ОШИБОК

## 📍 ГДЕ СМОТРЕТЬ ОШИБКИ

1. **Railway → Ваш проект**
2. **Выберите сервис** (API или Web)
3. **Вкладка "Deployments"** (или "Logs")
4. **Последний deployment → "View Logs"**

---

## ✅ ЧТО ДОЛЖНО БЫТЬ В ЛОГАХ API

### При успешной сборке:
```
✓ Prisma Client generated
✓ Build completed
```

### При успешном запуске:
```
🚀 API server is running on port 3001
✅ CORS enabled for: https://...
```

### ❌ Если видите ошибки:
- `Cannot connect to database` → Нет `DATABASE_URL`
- `JWT_SECRET is missing` → Нет `JWT_SECRET`
- `Prisma Client not generated` → Проблема со сборкой
- `Migration failed` → Проблема с БД

---

## 🔧 БЫСТРОЕ ИСПРАВЛЕНИЕ

### 1. Проверьте переменные окружения:

**API сервис → Variables → Должны быть:**

- ✅ `DATABASE_URL` = `${{Postgres.DATABASE_URL}}`
- ✅ `JWT_SECRET` = `DjLNj8omoOmhO0c2WuOC6pIqlO9FUlop+C3X4lwkJ9Y=`
- ✅ `FRONTEND_URL` = `https://ваш-web-домен.railway.app`
- ❌ `PORT` - НЕ добавляйте! Railway устанавливает автоматически

### 2. Если переменных нет:

1. API → Variables → "+ New Variable"
2. Добавьте каждую переменную
3. Railway автоматически перезапустит

### 3. Если переменные есть, но ошибка:

1. API → Settings → Restart
2. Подождите 1-2 минуты
3. Проверьте логи снова

---

## 📋 СКОПИРУЙТЕ И ПРИШЛИТЕ

Если ошибка не исправляется, пришлите:

1. **Текст ошибки** из логов (красные строки)
2. **Список переменных** (только названия, без значений)
3. **На каком этапе** (Build / Deploy / Runtime)

---

## 🚀 ПРОВЕРКА РАБОТЫ

После исправления:

1. Откройте Railway → API → Networking → Domain
2. Скопируйте URL
3. Откройте в браузере: `https://ваш-api.railway.app`
4. Должна быть ошибка 404 или JSON ответ (это нормально, значит API работает!)

