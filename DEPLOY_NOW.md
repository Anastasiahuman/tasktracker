# 🚀 ДЕПЛОЙ СЕЙЧАС - ПОШАГОВАЯ ИНСТРУКЦИЯ

## ✅ Что уже готово:

- ✅ Конфигурация Railway настроена
- ✅ NIXPACKS настроен для monorepo
- ✅ Все файлы закоммичены локально

## 📤 ШАГ 1: Push в GitHub

Выполните в терминале:
```bash
git push origin main
```

Или используйте токен (если нужно):
```bash
git remote set-url origin https://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@github.com/Anastasiahuman/tasktracker.git
git push origin main
git remote set-url origin https://github.com/Anastasiahuman/tasktracker.git
```

## 🔧 ШАГ 2: В Railway - PostgreSQL

1. Откройте ваш проект в Railway
2. Нажмите "+ New" → "Database" → "PostgreSQL"
3. Дождитесь статуса "Healthy"

## 🔧 ШАГ 3: В Railway - API сервис

### Переменные окружения (Variables):

1. Откройте API сервис → Variables
2. Добавьте каждую переменную:

**DATABASE_URL:**
```
${{Postgres.DATABASE_URL}}
```

**JWT_SECRET:**
```
DjLNj8omoOmhO0c2WuOC6pIqlO9FUlop+C3X4lwkJ9Y=
```

**PORT:**
```
3001
```

**FRONTEND_URL:**
```
(оставьте пустым пока)
```

### После добавления переменных:

1. Railway автоматически перезапустит сборку
2. Проверьте логи сборки
3. Должно быть: "✓ pnpm installed", "✓ build completed"

## 🌐 ШАГ 4: Получение URL

1. API сервис → Networking
2. Нажмите "Generate Domain"
3. Скопируйте URL (например: `https://xxx.up.railway.app`)

## ✅ ШАГ 5: Проверка

Откройте в браузере:
```
https://ваш-api-url.up.railway.app/health
```

Должно вернуть: `{"ok":true}`

## 🎉 ГОТОВО!

API задеплоен и работает!

## 📋 Следующий шаг: Web сервис

После успешного деплоя API:
1. Добавьте новый сервис из того же репо
2. Root Directory: `apps/web`
3. Переменные:
   - `NEXT_PUBLIC_USE_API` = `true`
   - `NEXT_PUBLIC_API_URL` = `https://ваш-api-url.up.railway.app`

