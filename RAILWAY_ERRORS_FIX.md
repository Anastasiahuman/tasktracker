# 🔧 ИСПРАВЛЕНИЕ ОШИБОК RAILWAY

## 📋 ЧЕКЛИСТ ПЕРЕМЕННЫХ ОКРУЖЕНИЯ

### ✅ API Сервис - Обязательные переменные:

1. **DATABASE_URL**
   - Где взять: Railway → PostgreSQL сервис → Variables → `DATABASE_URL`
   - Скопируйте значение
   - Вставьте в: API сервис → Variables → Добавить `DATABASE_URL`

2. **JWT_SECRET**
   - Значение: `DjLNj8omoOmhO0c2WuOC6pIqlO9FUlop+C3X4lwkJ9Y=`
   - Или сгенерируйте новый: `openssl rand -base64 32`

3. **FRONTEND_URL**
   - Это URL вашего Web сервиса
   - Формат: `https://ваш-домен.railway.app`
   - Если Web еще не задеплоен, можно временно: `http://localhost:3000`

4. **PORT**
   - Railway устанавливает автоматически
   - НЕ добавляйте вручную!

### 📧 Опционально (для invitations):

5. **RESEND_API_KEY**
   - Нужен только если используете email invitations
   - Получите на: https://resend.com/api-keys

---

## 🐛 ЧАСТЫЕ ОШИБКИ И РЕШЕНИЯ

### ❌ Ошибка 1: "Cannot connect to database"
**Причина:** Нет `DATABASE_URL` или неправильный формат

**Решение:**
1. Railway → PostgreSQL → Variables → `DATABASE_URL`
2. Скопируйте значение
3. API → Variables → Добавить `DATABASE_URL` = (вставьте значение)
4. Перезапустите API сервис

---

### ❌ Ошибка 2: "Prisma Client not generated"
**Причина:** Prisma Client не сгенерирован

**Решение:**
1. Проверьте логи сборки (Build logs)
2. Должно быть: `Prisma Client generated`
3. Если нет - проверьте `nixpacks.toml`:
   ```toml
   [phases.build]
   cmds = ["cd apps/api && pnpm prisma generate", "cd apps/api && pnpm build"]
   ```

---

### ❌ Ошибка 3: "Migration failed"
**Причина:** Миграции не применены

**Решение:**
1. API → Variables → Проверьте `DATABASE_URL`
2. API → Deployments → View Logs
3. Должно быть: `Migration applied successfully`
4. Если ошибка - проверьте подключение к БД

---

### ❌ Ошибка 4: "JWT_SECRET is missing"
**Причина:** Нет переменной `JWT_SECRET`

**Решение:**
1. API → Variables → Добавить
2. Имя: `JWT_SECRET`
3. Значение: `DjLNj8omoOmhO0c2WuOC6pIqlO9FUlop+C3X4lwkJ9Y=`
4. Перезапустите сервис

---

### ❌ Ошибка 5: "CORS error" (в браузере)
**Причина:** `FRONTEND_URL` не настроен или неправильный

**Решение:**
1. Узнайте URL Web сервиса: Railway → Web → Networking → Domain
2. API → Variables → Добавить `FRONTEND_URL` = `https://ваш-домен.railway.app`
3. Перезапустите API

---

### ❌ Ошибка 6: "pnpm: command not found"
**Причина:** pnpm не установлен

**Решение:**
1. Проверьте `nixpacks.toml`:
   ```toml
   [phases.install]
   cmds = ["corepack enable", "corepack prepare pnpm@latest --activate", "pnpm install --frozen-lockfile"]
   ```
2. Убедитесь, что Railway использует Nixpacks builder
3. Railway → API → Settings → Builder → NIXPACKS

---

### ❌ Ошибка 7: "Build failed" / "Module not found"
**Причина:** Зависимости не установлены или неправильная структура

**Решение:**
1. Проверьте логи сборки
2. Убедитесь, что `pnpm install` выполняется в корне проекта
3. Проверьте `package.json` в корне

---

## 🔍 КАК ПРОВЕРИТЬ ЛОГИ

1. **Railway → Ваш проект → API сервис**
2. **Вкладка "Deployments"** → Выберите последний deployment
3. **"View Logs"** или **"Logs"** вкладка
4. Ищите ошибки (красным цветом)

---

## ✅ БЫСТРАЯ ПРОВЕРКА

### API работает, если:
- В логах: `🚀 API server is running on port...`
- Нет ошибок (красных сообщений)
- Можно открыть: `https://ваш-api.railway.app/health` (если есть endpoint)

### База данных работает, если:
- PostgreSQL сервис показывает "Running"
- В логах API нет ошибок подключения к БД

### Web работает, если:
- В логах: `Ready on http://localhost:3000` или подобное
- Можно открыть домен в браузере
- Страница загружается

---

## 🚀 ПОШАГОВАЯ ПРОВЕРКА

1. ✅ PostgreSQL запущен и работает
2. ✅ API имеет переменную `DATABASE_URL` (из PostgreSQL)
3. ✅ API имеет переменную `JWT_SECRET`
4. ✅ API имеет переменную `FRONTEND_URL` (URL Web сервиса)
5. ✅ API успешно собрался (Build logs без ошибок)
6. ✅ API успешно запустился (Deploy logs: "API server is running")
7. ✅ Web имеет переменную `NEXT_PUBLIC_API_URL` (URL API сервиса)
8. ✅ Web успешно собрался и запустился

---

## 💡 ЕСЛИ НИЧЕГО НЕ ПОМОГАЕТ

1. **Скопируйте полный текст ошибки** из логов
2. **Проверьте все переменные окружения** (сверьте с этим списком)
3. **Перезапустите сервис**: Railway → API → Settings → Restart
4. **Проверьте GitHub**: Убедитесь, что код закоммичен и запушен

---

## 📞 ЧТО ПРИСЛАТЬ ДЛЯ ПОМОЩИ

Если нужна помощь, пришлите:
1. Скриншот ошибки из логов
2. Список переменных окружения (без значений, только имена)
3. На каком этапе ошибка (Build / Deploy / Runtime)

