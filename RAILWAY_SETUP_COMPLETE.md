# 🚀 ПОЛНАЯ НАСТРОЙКА RAILWAY - ПОШАГОВАЯ ИНСТРУКЦИЯ

## ✅ ЧТО УЖЕ ГОТОВО:

- ✅ Код запушен в GitHub
- ✅ Страница регистрации создана
- ✅ Конфигурация проекта готова

## 📋 ШАГ 1: ПОДКЛЮЧЕНИЕ GITHUB К RAILWAY

### Для Web сервиса:

1. **Откройте Railway Dashboard:**
   - https://railway.app
   - Войдите в аккаунт

2. **Выберите Web сервис:**
   - Найдите сервис "Web" в вашем проекте
   - Откройте его

3. **Подключите GitHub репозиторий:**
   - Settings → Source
   - Нажмите "Disconnect" (если есть старое подключение)
   - Нажмите "Connect Repo" или "Connect GitHub"
   - Railway откроет окно авторизации GitHub
   - **Нажмите "Authorize Railway"** в GitHub
   - Выберите репозиторий: **Anastasiahuman/tasktracker**
   - Нажмите "Connect"

4. **Настройте Root Directory:**
   - Settings → Source → Root Directory
   - Установите: **`/apps/web`**
   - Сохраните

### Для API сервиса:

1. **Выберите API сервис:**
   - Найдите сервис "API" в вашем проекте
   - Откройте его

2. **Подключите GitHub репозиторий:**
   - Settings → Source
   - Нажмите "Connect Repo" или "Connect GitHub"
   - Выберите репозиторий: **Anastasiahuman/tasktracker**
   - Нажмите "Connect"

3. **Настройте Root Directory:**
   - Settings → Source → Root Directory
   - Установите: **`/apps/api`**
   - Сохраните

## 📋 ШАГ 2: НАСТРОЙКА ПЕРЕМЕННЫХ ОКРУЖЕНИЯ

### Для API сервиса:

1. **Откройте API сервис → Variables**

2. **Добавьте переменные:**

   **DATABASE_URL:**
   ```
   ${{Postgres.DATABASE_URL}}
   ```
   (Railway автоматически подставит URL базы данных)

   **JWT_SECRET:**
   ```
   DjLNj8omoOmhO0c2WuOC6pIqlO9FUlop+C3X4lwkJ9Y=
   ```

   **PORT:**
   ```
   3001
   ```

   **FRONTEND_URL:**
   ```
   (оставьте пустым пока, добавите после получения URL Web сервиса)
   ```

### Для Web сервиса:

1. **Откройте Web сервис → Variables**

2. **Добавьте переменные:**

   **NEXT_PUBLIC_API_URL:**
   ```
   https://ваш-api-url.up.railway.app
   ```
   (Замените на реальный URL вашего API сервиса)

   **NEXT_PUBLIC_USE_API:**
   ```
   true
   ```

## 📋 ШАГ 3: НАСТРОЙКА BUILD И START КОМАНД

### Для API сервиса:

1. **Settings → Deploy**

2. **Build Command:**
   ```
   npm install --frozen-lockfile && cd apps/api && npx prisma generate && npm run build
   ```

3. **Start Command:**
   ```
   cd apps/api && npx prisma migrate deploy && npm run start:prod
   ```

### Для Web сервиса:

1. **Settings → Deploy**

2. **Build Command:**
   ```
   npm ci && cd apps/web && npm run build
   ```
   (Или оставьте пустым - Railway определит автоматически для Next.js)

3. **Start Command:**
   ```
   cd apps/web && npm start
   ```

## 📋 ШАГ 4: ПОЛУЧЕНИЕ ПУБЛИЧНЫХ URL

### Для API сервиса:

1. **API сервис → Networking**
2. Нажмите "Generate Domain" или "Add Domain"
3. Скопируйте URL (например: `https://api-production-xxxx.up.railway.app`)

### Для Web сервиса:

1. **Web сервис → Networking**
2. Нажмите "Generate Domain" или "Add Domain"
3. Скопируйте URL (например: `https://web-production-xxxx.up.railway.app`)

## 📋 ШАГ 5: ОБНОВЛЕНИЕ ПЕРЕМЕННЫХ ОКРУЖЕНИЯ

### Обновите FRONTEND_URL в API сервисе:

1. **API сервис → Variables**
2. Найдите **FRONTEND_URL**
3. Установите значение: URL вашего Web сервиса
4. Сохраните

### Обновите NEXT_PUBLIC_API_URL в Web сервисе:

1. **Web сервис → Variables**
2. Найдите **NEXT_PUBLIC_API_URL**
3. Установите значение: URL вашего API сервиса
4. Сохраните

## 📋 ШАГ 6: ПРОВЕРКА

### Проверьте API:

1. Откройте: `https://ваш-api-url.up.railway.app/health`
2. Должно вернуть: `{"ok":true}`

### Проверьте Web:

1. Откройте: `https://ваш-web-url.up.railway.app`
2. Должна открыться главная страница
3. Проверьте страницу регистрации: `https://ваш-web-url.up.railway.app/register`

## ✅ ГОТОВО!

После выполнения всех шагов проект должен работать полностью!

## 🔧 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ:

1. Проверьте логи в Railway:
   - API сервис → Deployments → Logs
   - Web сервис → Deployments → Logs

2. Убедитесь, что все переменные окружения установлены

3. Проверьте, что Root Directory правильный:
   - API: `/apps/api`
   - Web: `/apps/web`

4. Убедитесь, что PostgreSQL сервис создан и работает

