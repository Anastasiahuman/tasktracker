# 🔑 Использование токена для push

## Вариант 1: Интерактивный скрипт (рекомендуется)

```bash
./push-with-token.sh
```

Скрипт попросит ввести токен (ввод будет скрыт).

## Вариант 2: Прямая передача токена

```bash
./push-direct.sh YOUR_TOKEN_HERE
```

Или через переменную окружения:
```bash
GITHUB_TOKEN=your_token_here ./push-direct.sh
```

## Вариант 3: Напрямую в команде git

```bash
git push https://YOUR_TOKEN@github.com/Anastasiahuman/tasktracker.git main
```

**Замените `YOUR_TOKEN` на ваш Personal Access Token!**

## Как получить токен:

1. Откройте: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Название: `task-tracker-deploy`
4. Выберите scope: **`repo`** (полный доступ)
5. Нажмите **"Generate token"**
6. **Скопируйте токен** (показывается только один раз!)

## После успешного push:

1. Откройте **RAILWAY_SETUP.md** - деплой API
2. Откройте **VERCEL_SETUP.md** - деплой Web

