# 🚀 ДЕПЛОЙ НА RAILWAY - ПОШАГОВАЯ ИНСТРУКЦИЯ

## ✅ ПОДГОТОВКА

Все файлы готовы к деплою:
- ✅ `railway.json` - конфигурация Railway
- ✅ `nixpacks.toml` - конфигурация сборки
- ✅ `package.json` - корневой файл для monorepo
- ✅ API готов к сборке
- ✅ Health Check endpoint создан

---

## 📋 ШАГ 1: ЗАПУШИТЬ КОД В GITHUB

### 1.1 Проверить статус Git

```bash
cd /Users/anastasiaamanova/task-tracker-cute
git status
```

### 1.2 Добавить все изменения

```bash
git add .
```

### 1.3 Закоммитить изменения

```bash
git commit -m "Add API integration and Railway deployment config"
```

### 1.4 Запушить в GitHub

```bash
git push origin main
```

Если нужен токен (из прошлого раза):
```bash
git remote set-url origin https://ghp_YOUR_TOKEN@github.com/Anastasiahuman/tasktracker.git
git push origin main
git remote set-url origin https://github.com/Anastasiahuman/tasktracker.git
```

---

## 📋 ШАГ 2: СОЗДАТЬ ПРОЕКТ НА RAILWAY

### 2.1 Войти на Railway

1. Откройте https://railway.app
2. Войдите через GitHub

### 2.2 Создать новый проект

1. Нажмите **"+ New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите репозиторий `Anastasiahuman/tasktracker`
4. Нажмите **"Deploy Now"**

---

## 📋 ШАГ 3: ДОБАВИТЬ POSTGRESQL БАЗУ ДАННЫХ

### 3.1 Добавить PostgreSQL

1. В проекте Railway нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Дождитесь статуса **"Healthy"**

### 3.2 Скопировать DATABASE_URL

1. Кликните на PostgreSQL сервис
2. Перейдите в **"Variables"**
3. Найдите `DATABASE_URL` (он создан автоматически)
4. Скопируйте значение (понадобится позже)

---

## 📋 ШАГ 4: НАСТРОИТЬ API СЕРВИС

### 4.1 Настроить сервис

1. Кликните на сервис с вашим репозиторием (автоматически создан)
2. Перейдите в **"Settings"**
3. Найдите **"Root Directory"**
4. Установите: `apps/api`
5. Сохраните изменения

### 4.2 Добавить переменные окружения

Перейдите в **"Variables"** и добавьте:

#### DATABASE_URL
```
${{Postgres.DATABASE_URL}}
```
(Используйте синтаксис Railway для ссылки на PostgreSQL)

#### JWT_SECRET
```
DjLNj8omoOmhO0c2WuOC6pIqlO9FUlop+C3X4lwkJ9Y=
```
(Или сгенерируйте новый: `openssl rand -base64 32`)

#### FRONTEND_URL
```
(оставьте пустым пока, добавим после деплоя фронтенда)
```

#### PORT
```
(НЕ устанавливайте - Railway автоматически назначает порт)
```

### 4.3 Дождаться деплоя

1. Railway автоматически начнет сборку после добавления переменных
2. Проверьте логи сборки в разделе **"Deployments"**
3. Дождитесь статуса **"Success"**

---

## 📋 ШАГ 5: ПРОВЕРИТЬ API

### 5.1 Получить URL API

1. Перейдите в **"Settings"** → **"Networking"**
2. Нажмите **"Generate Domain"**
3. Скопируйте URL (например: `https://xxx.up.railway.app`)

### 5.2 Проверить Health Check

Откройте в браузере:
```
https://ваш-api-url.up.railway.app/health
```

Должно вернуть:
```json
{
  "ok": true,
  "timestamp": "2025-12-29T...",
  "service": "task-tracker-api"
}
```

---

## 📋 ШАГ 6: ДЕПЛОЙ FRONTEND (ОПЦИОНАЛЬНО)

Frontend можно задеплоить на Railway или на Vercel.

### Вариант A: Railway

1. В том же проекте нажмите **"+ New"**
2. Выберите **"GitHub Repo"** → выберите тот же репозиторий
3. В настройках установите **Root Directory**: `apps/web`
4. Добавьте переменную окружения:
   - `NEXT_PUBLIC_API_URL` = `https://ваш-api-url.up.railway.app`
5. Дождитесь деплоя

### Вариант B: Vercel (рекомендуется для Next.js)

1. Откройте https://vercel.com
2. Импортируйте репозиторий
3. Установите **Root Directory**: `apps/web`
4. Добавьте переменную:
   - `NEXT_PUBLIC_API_URL` = `https://ваш-api-url.up.railway.app`
5. Деплой автоматически

---

## 📋 ШАГ 7: ФИНАЛЬНАЯ НАСТРОЙКА

### 7.1 Обновить FRONTEND_URL в API

После деплоя фронтенда:

1. Вернитесь в API сервис на Railway
2. Перейдите в **"Variables"**
3. Обновите `FRONTEND_URL` на URL фронтенда
4. Railway автоматически перезапустит API

### 7.2 Проверить работу

1. Откройте фронтенд в браузере
2. Попробуйте зарегистрироваться/войти
3. Создайте задачу
4. Проверьте, что все работает

---

## 🔧 УСТРАНЕНИЕ ПРОБЛЕМ

### Ошибка сборки

1. Проверьте логи в Railway
2. Убедитесь, что `Root Directory` установлен на `apps/api`
3. Проверьте, что все переменные окружения установлены

### Ошибка подключения к БД

1. Проверьте, что PostgreSQL запущен (статус "Healthy")
2. Убедитесь, что `DATABASE_URL` правильно установлен
3. Проверьте формат: `${{Postgres.DATABASE_URL}}`

### Ошибка CORS

1. Убедитесь, что `FRONTEND_URL` установлен правильно
2. URL должен быть без слеша в конце
3. Перезапустите API после изменения

---

## ✅ ЧЕКЛИСТ

- [ ] Код запушен в GitHub
- [ ] Проект создан на Railway
- [ ] PostgreSQL база данных добавлена
- [ ] API сервис настроен (Root: apps/api)
- [ ] Переменные окружения установлены:
  - [ ] DATABASE_URL
  - [ ] JWT_SECRET
  - [ ] FRONTEND_URL (опционально)
- [ ] API успешно задеплоен
- [ ] Health Check работает
- [ ] Frontend задеплоен (Railway или Vercel)
- [ ] FRONTEND_URL обновлен в API
- [ ] Приложение работает полностью

---

## 📚 ПОЛЕЗНЫЕ ССЫЛКИ

- Railway Dashboard: https://railway.app/dashboard
- Railway Docs: https://docs.railway.app
- Vercel Dashboard: https://vercel.com/dashboard

---

## 💡 СОВЕТЫ

1. **Начните с API** - сначала задеплойте API и проверьте его работу
2. **Используйте Health Check** - убедитесь, что API работает перед деплоем фронтенда
3. **Проверяйте логи** - Railway предоставляет детальные логи для отладки
4. **Сохраните URL** - запишите URL API и Frontend для будущего использования

