# 📋 ДАННЫЕ ДЛЯ НАСТРОЙКИ RAILWAY (ШАГ 4)

## 🔧 НАСТРОЙКА API СЕРВИСА

### Root Directory
```
apps/api
```

### Переменные окружения (Variables)

#### 1. DATABASE_URL
```
${{Postgres.DATABASE_URL}}
```
**Важно:** Используйте синтаксис Railway для ссылки на PostgreSQL сервис

#### 2. JWT_SECRET
```
DjLNj8omoOmhO0c2WuOC6pIqlO9FUlop+C3X4lwkJ9Y=
```
**Примечание:** Или сгенерируйте новый секретный ключ:
```bash
openssl rand -base64 32
```

#### 3. FRONTEND_URL
```
(оставьте пустым пока, добавим после деплоя фронтенда)
```

#### 4. PORT
```
(НЕ устанавливайте - Railway автоматически назначает порт через переменную PORT)
```

---

## 📝 ИНСТРУКЦИЯ ДЛЯ ШАГА 4

### 4.1 Настроить сервис

1. В проекте Railway кликните на сервис с вашим репозиторием (автоматически создан при импорте)
2. Перейдите в **"Settings"** (Настройки)
3. Найдите поле **"Root Directory"** (Корневая директория)
4. Введите: `apps/api`
5. Нажмите **"Save"** (Сохранить)

### 4.2 Добавить переменные окружения

1. Перейдите в раздел **"Variables"** (Переменные)
2. Нажмите **"+ New Variable"** (Новая переменная)
3. Добавьте каждую переменную:

   **DATABASE_URL:**
   - Name: `DATABASE_URL`
   - Value: `${{Postgres.DATABASE_URL}}`
   - Нажмите "Add"

   **JWT_SECRET:**
   - Name: `JWT_SECRET`
   - Value: `DjLNj8omoOmhO0c2WuOC6pIqlO9FUlop+C3X4lwkJ9Y=`
   - Нажмите "Add"

4. Railway автоматически начнет новую сборку после добавления переменных

### 4.3 Проверить деплой

1. Перейдите в раздел **"Deployments"** (Деплои)
2. Дождитесь статуса **"Success"** (Успешно)
3. Проверьте логи, если есть ошибки

---

## ✅ ЧЕКЛИСТ

- [ ] Root Directory установлен: `apps/api`
- [ ] DATABASE_URL добавлен: `${{Postgres.DATABASE_URL}}`
- [ ] JWT_SECRET добавлен: (значение выше)
- [ ] FRONTEND_URL оставлен пустым (пока)
- [ ] PORT НЕ установлен (Railway автоматически)
- [ ] Деплой успешно завершен
- [ ] Логи проверены (нет ошибок)

---

## 🔍 ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### Проблема: "Root Directory not found"
**Решение:** Убедитесь, что репозиторий подключен и содержит директорию `apps/api`

### Проблема: "DATABASE_URL is invalid"
**Решение:** 
1. Убедитесь, что PostgreSQL сервис создан
2. Используйте синтаксис `${{Postgres.DATABASE_URL}}` (с двойными фигурными скобками)
3. Имя сервиса PostgreSQL должно быть именно "Postgres" (или используйте имя вашего сервиса)

### Проблема: "Build failed"
**Решение:**
1. Проверьте логи в разделе "Deployments"
2. Убедитесь, что Root Directory правильный
3. Проверьте, что все переменные окружения установлены

---

## 📚 ПОЛЕЗНЫЕ ССЫЛКИ

- Railway Dashboard: https://railway.app/dashboard
- Railway Docs (Variables): https://docs.railway.app/develop/variables
- Railway Docs (Root Directory): https://docs.railway.app/develop/services#root-directory

